import React from 'react';

const LoadingComponent = () => {
  return (
    <div className="error-flex">
      <div className="error-flex-item text-tip">加载中...</div>
    </div>
  );
};

export default LoadingComponent;
